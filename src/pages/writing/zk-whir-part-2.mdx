---
layout: ../../components/blog-post.astro
title: "Zero-Knowledge WHIR II: Code-Switching in the Dark"
description: "Part 2 of 2 on Chiesa–Fenzi–Weissenberg's HVZK IOPP. Zero-knowledge code-switching via private zero-evaders, and assembly into HVZK WHIR/Ligerito at 1+o(1) overhead."
pubDate: "2026-05-30"
tags: ["zero-knowledge", "snarks", "iops", "whir"]
---

import ProtocolStepper from "../../components/protocol-stepper.astro";

## Where Part 1 left us

[Part 1](/writing/zk-whir-part-1) built the foundations of an HVZK IOPP for constrained interleaved codes:

- **Zero-knowledge encodings.** Randomized encodings $\mathsf{Enc}_{\mathcal C} \colon \mathbb F^\ell \times \mathbb F^r \to \Sigma^m$ where any $\leq t$ coordinates of the codeword can be simulated from positions alone.
- **The committed sumcheck relation $\mathcal R_{\mathcal C, \mathcal C_{\mathsf{zk}}, \mathsf{sl}}$.** Succinct linear forms to kill the verifier's $\Omega(\ell)$ cost, plus small mask oracles folded into a joint constraint to hide the target $\mu$.
- **Composable HVZK for IORs.** The standard HVZK definition breaks under composition (Part 1's $\mathsf{IOR}_1 \circ \mathsf{IOR}_2$ counterexample). Fix: a distinguisher class $\mathcal D$ that the simulator must additionally handle, and a composition theorem that chains errors $\zeta_1 + \zeta_2$ across the seam.
- **HVZK sumcheck reduction.** Reduces a $2^k$-interleaved code to its base code via small per-round masks (not a full $2\times$ mask), with mask oracles carried forward through the joint constraint. Drops problem size by a factor of $2^k$ at sublinear masking cost.

We can now test the _interleaving_ of any zero-knowledge code in zero knowledge — by reducing to the base code. But the base code is still big. To get a polylog verifier we need to recursively shrink the code, and the move that does that is **code-switching**: an IOR from a test for code $\mathcal C$ to a test for some smaller code $\mathcal C'$.

The non-zero-knowledge version is well-trodden ([RR20]; [BCFW25]; WHIR's recursion shape). The zero-knowledge version breaks in two distinct places. This part fixes both, assembles the HVZK WHIR/Ligerito corollary, and tackles the R1CS reduction.

## Code-switching without zero-knowledge

Code-switching follows a clean template. The verifier has an oracle $f$ that — honestly — equals $\mathcal C(\boldsymbol m)$ for some message $\boldsymbol m$ satisfying a predicate $\Phi$. The prover wants to convince the verifier of this fact using a _smaller_ proximity test.

1. **Prover sends $g$.** A new oracle $g \in (\Sigma')^{m'}$. In the honest case, $g = \mathcal C'(\boldsymbol m)$ — the _same_ message encoded under the new, smaller code $\mathcal C'$.
2. **Verifier spot-queries $f$.** Sample $x_1, \dots, x_t \in [m]$, query $f(x_1), \dots, f(x_t)$.
3. **Run a proximity protocol** for the new claim "$g$ encodes some $\boldsymbol m$ such that $\Phi(\boldsymbol m) = 1$ and $\mathcal C(\boldsymbol m)[x_i] = f(x_i)$ for every $i$."

The added clauses $\mathcal C(\boldsymbol m)[x_i] = f(x_i)$ are the **consistency check**: they tie the new oracle's message to the old oracle's message. Without them, the prover could swap $\boldsymbol m$ at the switch.

This template is everywhere — [RR20], [RR22], [BCFW25], the [BMMS25] family of FICS/FACS protocols, WHIR's recursion. It has _not_ been applied to zero-knowledge codes outside [RW24], whose construction leans on specific properties of zero-knowledge tensor codes. We need a code-agnostic ZK version.

## Why ZK encodings break the template

The first break: the consistency check assumes the codeword is a function of the message alone. With a ZK encoding it isn't.

Concretely, $f = \mathsf{Enc}_{\mathcal C}(\boldsymbol m, \boldsymbol r)$ depends on _both_ the message and the randomness. The consistency check $\mathcal C(\boldsymbol m)[x_i] = f(x_i)$ doesn't even type-check anymore — you need $\boldsymbol r$ to predict $f$.

The obvious fix: have $g$ encode both the message and the randomness under $\mathcal C'$. That works, but it requires padding $g$'s message space, which means $g$ is twice the size, every subsequent sumcheck operates on a $2\times$-blown object, and we're back to $\geq 2\times$ overhead. Dead.

The actual fix reuses the committed sumcheck relation. Introduce an _additional mask_ $s = \mathsf{Enc}_{\mathcal C_{\mathsf{zk}}}(\boldsymbol r, \boldsymbol r'')$ encoding the randomness $\boldsymbol r$ under the ZK mask code $\mathcal C_{\mathsf{zk}}$. Now write the per-symbol claim by splitting $\mathsf{Enc}_{\mathcal C}$'s generator matrix into a message part and a randomness part: $G_{\mathcal C} = [G_{\mathcal C}^{\mathrm{msg}} \mid G_{\mathcal C}^{\mathrm{rnd}}]$ where $G_{\mathcal C}^{\mathrm{msg}}$ acts on the message and $G_{\mathcal C}^{\mathrm{rnd}}$ acts on the randomness. Then

$$
f(x) \;=\; \mathsf{Enc}_{\mathcal C}(\boldsymbol m, \boldsymbol r)[x] \;=\; \langle \boldsymbol m,\, G_{\mathcal C}^{\mathrm{msg}}[x] \rangle + \langle \boldsymbol r,\, G_{\mathcal C}^{\mathrm{rnd}}[x] \rangle,
$$

which is a linear constraint on $(\boldsymbol m, \boldsymbol r)$. The first piece accumulates into the existing main-oracle constraint via the succinct linear form $\mathsf{sl}'$; the second piece accumulates as a constraint on the mask $s$. Both go into the joint constraint of the next committed sumcheck relation. No padding, no $2\times$.

That handles the randomness break. But the bigger break is structural: the WHIR-style recursion uses **out-of-domain sampling** in the list-decoding regime, and OOD's whole _job_ is to leak information.

## OOD sampling and the second tension

Quick recap (see the [WHIR post](/writing/whir) for the proper treatment). In the list-decoding regime — where the verifier picks a proximity parameter $\delta$ larger than the unique-decoding radius — multiple codewords in $\Lambda(\mathcal C, f, \delta)$ can be close to a malicious oracle $f$. The verifier needs a way to force the prover to commit to _one_ codeword, not a list.

WHIR's version of the fix: the codewords are evaluations of low-degree polynomials, so the verifier sends a random point $z$ _outside_ the evaluation domain and the prover answers with a claimed value $\hat y$. Two distinct low-degree polynomials agree at a random point with probability at most $d/|\mathbb F|$, so with high probability at most one polynomial in the list is consistent with $(z, \hat y)$ — by answering, the prover has picked one. That's an **out-of-domain (OOD) sample**.

The code-agnostic version abstracts the only property this used: evaluation at $z$ is a _linear function of the message_ — the inner product of the coefficient vector with $(1, z, z^2, \dots)$. So replace "evaluate at a random point" with "answer a random linear probe." The verifier samples randomness $\rho \leftarrow D_{\mathsf{ze}}$, which determines a public matrix $\mathsf{ze}(\rho) \in \mathbb F^{t_{\mathsf{ood}} \times \ell}$ ($\mathsf{ze}$ for _zero-evader_ — defined next section), and the prover answers

$$
\boldsymbol y \;:=\; \mathsf{ze}(\rho) \cdot \boldsymbol m \;\in\; \mathbb F^{t_{\mathsf{ood}}},
$$

$t_{\mathsf{ood}}$ random linear combinations of the message coordinates. The verifier's later checks enforce that the message it ends up trusting answers the probe with exactly $\boldsymbol y$. This pins one codeword for the same reason the random point did: two distinct list codewords have distinct messages, their difference is a fixed nonzero vector, and a random probe sends a fixed nonzero vector to $\boldsymbol 0$ only with small probability — so no two list elements share an answer, and $\boldsymbol y$ selects at most one.

Now look at what the prover just sent: $t_{\mathsf{ood}}$ linear combinations of $\boldsymbol m$ with public coefficients, _in the clear_. **$\boldsymbol y$ leaks information about $\boldsymbol m$, hence about the witness.**

And the leak is _inherent_. OOD is designed to uniquely identify the codeword (i.e., the witness). Zero-knowledge forbids revealing the witness. The two cannot literally coexist — you have to weaken one.

The paper weakens neither. It restructures OOD so the leak hits randomness instead of the witness.

## Zero-evaders and private zero-evaders

The revised OOD sample is

$$
\boldsymbol y \;:=\; \mathsf{ze}(\rho) \cdot \begin{bmatrix} \boldsymbol m \\ \boldsymbol r \end{bmatrix},
$$

with the difference being that the probe now also covers _freshly sampled_ randomness $\boldsymbol r \leftarrow \mathbb F^r$ — not the encoding coins, new randomness whose only job is privacy. The function $\mathsf{ze}$ is asked to satisfy two conditions:

1. **It's still a good OOD sample.** Equivalently, $\mathsf{ze}$ is a **zero-evader** [BCFW25]: for every nonzero vector $\boldsymbol v$,
   $$\Pr_{\rho \leftarrow D_{\mathsf{ze}}}\big[\mathsf{ze}(\rho) \cdot \boldsymbol v = \boldsymbol 0\big] \leq \varepsilon_{\mathsf{zero}}.$$
   I.e. the matrix $\mathsf{ze}(\rho)$ is, in expectation, full-rank enough to detect any nonzero input difference. This is what enables list-decoding-regime soundness.
2. **It leaks no information about $\boldsymbol m$.** Formalized: $\mathsf{ze}$ is an **$(r, \zeta_{\mathsf{ze}})$-private zero-evader** if $(\rho, \boldsymbol y)$ can be efficiently simulated, with statistical error at most $\zeta_{\mathsf{ze}}$, _without_ knowing $\boldsymbol m$.

Condition (2) sounds abstract; the construction that achieves it is not. Take any zero-evader and append $r$ columns sampled as a random rank-$t_{\mathsf{ood}}$ matrix $M \in \mathbb F^{t_{\mathsf{ood}} \times r}$:

$$
\mathsf{ze}'(\rho, M) := \big[\, \mathsf{ze}(\rho) \mid M \,\big], \qquad
\boldsymbol y = \mathsf{ze}(\rho) \cdot \boldsymbol m + M \boldsymbol r.
$$

This is the surjectivity argument from Part 1's ZK encodings again: $M$'s columns span $\mathbb F^{t_{\mathsf{ood}}}$, so with $\boldsymbol r$ uniform, $M\boldsymbol r$ is uniform — a one-time pad on top of $\mathsf{ze}(\rho) \cdot \boldsymbol m$. Then $\boldsymbol y$ is uniform even conditioned on $(\rho, M)$, and the simulator just outputs a uniform $\boldsymbol y$. Zero-evasion survives: a nonzero $(\boldsymbol v, \boldsymbol w)$ with $\boldsymbol w \neq \boldsymbol 0$ is caught by the random $M$ except with probability $\tfrac{1}{|\mathbb F|^{t_{\mathsf{ood}}} - 1}$; with $\boldsymbol w = \boldsymbol 0$ it reduces to the original $\mathsf{ze}$.

> **Lemma (informal, Lemma 2.13; formally Lemma 9.3).** Any zero-evader with error $\varepsilon_{\mathsf{zero}}$ extends, for any $r \geq t_{\mathsf{ood}}$, to an $(r, 0)$-private zero-evader with error $\approx \varepsilon_{\mathsf{zero}}$. **Perfect privacy costs nothing.**

<img
  src="/diagrams/zk-whir-ood-vs-private-ood.svg"
  alt="Side-by-side comparison: left, non-ZK OOD computes y = ze(ρ) · m and ships y in the clear, leaking a linear function of the witness message; right, private-zero-evader OOD computes y = ze(ρ) · (m, r) by mixing m with freshly sampled randomness r, so y is uniform from the verifier's view while ze still pins a unique message."
/>

### What the constraint can — and can't — pin

Recall what the OOD constraint does with the list: a candidate codeword in $\Lambda(\mathcal C, f, \delta)$ _survives_ if its message answers the probe with $\boldsymbol y$. In the non-ZK world the encoding is a bijection — one message, one codeword — so "at most one surviving message" and "at most one surviving codeword" are the same statement, and [BCFW25] proves it.

A ZK encoding breaks the bijection. Decoding a candidate $\bar f \in \Lambda(\mathcal C, f, \delta)$ now yields a _pair_ $(\bar{\boldsymbol m}, \bar{\boldsymbol r}) = \mathsf{Enc}_{\mathcal C}^{-1}(\bar f)$ — message part and coins — and the probe is applied to the message part only. So list elements can differ in two ways, and the constraint treats them very differently:

- **Same message, different coins.** Both answer the probe identically. Both survive — the constraint _cannot_ separate them. And it must not: privacy requires the honest codeword to be indistinguishable among all encodings of $\boldsymbol m$; a constraint that singled out one encoding would itself leak the coins.
- **Different messages.** The message difference is a nonzero vector, and a zero-evader kills nonzero vectors: they share an answer with probability at most $\varepsilon_{\mathsf{zero}}$.

So the constraint partitions the list into classes — one per message — and with high probability at most one _class_ is consistent with $\boldsymbol y$. The message is pinned; which encoding of it sits near $f$ stays ambiguous. That is exactly the split we need: soundness pinned the thing it uses, privacy kept the thing it hides behind.

Why is a pinned message enough, when the non-ZK analysis pinned a codeword? Because "which codeword" was never the question. Everything downstream is a statement about the message: the predicate $\Phi(\boldsymbol m)$, consistency between $f$ and $g$ across the switch, the witness the extractor must output. The non-ZK proofs pinned the codeword only because there it was the same thing.

Formally, stated for the joint probe over oracle-and-mask pairs:

> **Lemma (informal, Lemma 2.14).** Take any two candidates $(\bar f_1, \bar s_1), (\bar f_2, \bar s_2)$ with $\bar f_i \in \Lambda(\mathcal C, f, \delta)$, $\bar s_i \in \Lambda(\mathcal C_{\mathsf{zk}}, s, \delta_{\mathsf{zk}})$ that answer the probe identically. Then their decoded message parts are equal, except with probability $\varepsilon_{\mathsf{zero}}$ for that fixed pair — and _some_ bad pair exists anywhere in the lists with probability at most $|\Lambda(\mathcal C, \delta)|^2 \cdot |\Lambda(\mathcal C_{\mathsf{zk}}, \delta_{\mathsf{zk}})|^2 \cdot \varepsilon_{\mathsf{zero}}$ over $\rho$. The coins are unconstrained.

The proof is the union bound you'd guess: fix two candidates whose message parts differ, zero-evasion kills their difference except with probability $\varepsilon_{\mathsf{zero}}$, union over all pairs of list elements.

<img
  src="/diagrams/zk-whir-lemma-2-14-equivalence.svg"
  alt="Equivalence-class diagram for Lemma 2.14: the product list Λ(C,δ) × Λ(C_zk, δ_zk) of nearby (codeword, mask) pairs is partitioned into colored classes by the underlying message; many distinct (f_i, s_i) pairs may satisfy the OOD constraint, but the lemma forces every satisfying pair into one message class, so soundness still pins the message even though the codeword is no longer unique."
/>

There's one last knot. The fresh randomness $\boldsymbol r$ must be _included_ in the code-switch claim too — otherwise the next round won't know about it. Padding $\boldsymbol r$ into $\mathcal C'$ would reintroduce the $2\times$ overhead we were just trying to avoid. The fix: a **joint OOD sample** of both $g$ and the mask $s$, treating the pair as one object. The committed sumcheck relation, with its accumulating joint constraint, is exactly the right vehicle for this — and the entire reason it was designed the way it was.

## The code-switch construction

Re-anchor what the IOR starts from, because everything below refers to it. The input is one committed-sumcheck claim over $(\mathcal C, \mathcal C_{\mathsf{zk}}, \mathsf{sl})$ — Part 1's relation:

$$
f = \mathsf{Enc}_{\mathcal C}(\boldsymbol m, \boldsymbol r),
\qquad
\langle \boldsymbol m, \mathsf{sl}(\mathsf{st}) \rangle + \sum_i \langle \boldsymbol \xi_i, \boldsymbol u_i \rangle = \mu.
$$

Concretely the verifier holds: the **oracle $f$**, hiding message $\boldsymbol m$ behind coins $\boldsymbol r$; the **mask oracles $\xi_i$** left behind by earlier rounds, hiding messages $\boldsymbol \xi_i$; and the **public data** — state $\mathsf{st}$, target $\mu$, and the coefficient vectors $\boldsymbol u_i$ each mask enters through. The output must be a claim of the _same shape_, one size smaller: a new oracle under $\mathcal C'$, one more mask, a new target $\mu'$, over $(\mathcal C', \mathcal C_{\mathsf{zk}}, \mathsf{sl}')$. That closure is what lets the step recurse.

The four pieces built so far — ZK-encoding-aware consistency, the joint OOD sample, the private zero-evader, and the committed sumcheck relation as the carrier — slot together like this:

<ProtocolStepper label="One round of recursion, step by step">

  <div class="space-y-1 border-b border-neutral-200 dark:border-neutral-800 py-3 text-neutral-500 dark:text-neutral-400">
    <div><span class="font-semibold text-neutral-900 dark:text-neutral-100">Prover</span> — knows $\boldsymbol m, \boldsymbol r$, and will sample fresh randomness $\boldsymbol r', \boldsymbol s, \boldsymbol r''$</div>
    <div><span class="font-semibold text-neutral-900 dark:text-neutral-100">Verifier</span> — holds $\mu, \mathsf{st}, (\boldsymbol u_i)$, and reaches $f, (\xi_i)$ only by query</div>
  </div>

  <div data-step class="border-b border-neutral-200 dark:border-neutral-800 py-4">
    <div class="mb-2 text-sm font-medium tracking-wide text-neutral-400 dark:text-neutral-500">Step 1 &nbsp;·&nbsp; Prover</div>
    Re-encode the _same_ message under the smaller code, with fresh coins: $g := \mathsf{Enc}_{\mathcal C'}(\boldsymbol m, \boldsymbol r')$. Then Part 1's mask-oracle move — a small committed codeword whose message enters the joint constraint — reused for a new job: commit $s := \mathsf{Enc}_{\mathcal C_{\mathsf{zk}}}((\boldsymbol s, \boldsymbol r), \boldsymbol r'')$, whose message is $f$'s old coins $\boldsymbol r$ padded with fresh randomness $\boldsymbol s$. The mask is how $\boldsymbol r$ reaches the next round <em>without</em> being padded into $\mathcal C'$ — the padding route was the $2\times$ dead end.
    <div class="mt-3 flex items-baseline gap-2.5 text-neutral-600 dark:text-neutral-300"><span class="shrink-0 text-neutral-400 dark:text-neutral-600">→</span><span>new oracle $g$, mask oracle $s$</span></div>
  </div>

  <div data-step class="border-b border-neutral-200 dark:border-neutral-800 py-4">
    <div class="mb-2 text-sm font-medium tracking-wide text-neutral-400 dark:text-neutral-500">Step 2 &nbsp;·&nbsp; Verifier</div>
    Draw the OOD randomness. $\rho$ selects the public probe matrix $\mathsf{ze}(\rho)$ — the random-linear-probe generalization of WHIR's out-of-domain point, needed because we're in the list-decoding regime and must force one candidate out of $\Lambda(\mathcal C', g, \delta)$.
    <div class="mt-3 flex items-baseline gap-2.5 text-neutral-600 dark:text-neutral-300"><span class="shrink-0 text-neutral-400 dark:text-neutral-600">←</span><span>OOD randomness $\rho \leftarrow D_{\mathsf{ze}}$</span></div>
  </div>

  <div data-step class="border-b border-neutral-200 dark:border-neutral-800 py-4">
    <div class="mb-2 text-sm font-medium tracking-wide text-neutral-400 dark:text-neutral-500">Step 3 &nbsp;·&nbsp; Prover</div>
    Answer the probe _jointly_ over everything the next round must pin — the message and both pieces of the mask's message:

$$
\boldsymbol y \;:=\; \mathsf{ze}(\rho) \cdot (\boldsymbol m, \boldsymbol r, \boldsymbol s)^\top \;\in\; \mathbb F^{t_{\mathsf{ood}}}
$$

    <div class="text-neutral-500 dark:text-neutral-400">$\mathsf{ze}$ is a private zero-evader: the fresh $\boldsymbol s$ inside the probe plays the one-time-pad role, so $\boldsymbol y$ is uniform and leaks nothing — yet by Lemma 2.14 it still pins the decoded message parts.</div>
    <div class="mt-3 flex items-baseline gap-2.5 text-neutral-600 dark:text-neutral-300"><span class="shrink-0 text-neutral-400 dark:text-neutral-600">→</span><span>joint OOD answer $\boldsymbol y$</span></div>
  </div>

  <div data-step class="border-b border-neutral-200 dark:border-neutral-800 py-4">
    <div class="mb-2 text-sm font-medium tracking-wide text-neutral-400 dark:text-neutral-500">Step 4 &nbsp;·&nbsp; Verifier</div>
    Spot-check that $g$'s message really is $f$'s message: sample query positions on the old oracle, plus batching scalars. Random $\boldsymbol \varepsilon$ fuses many linear constraints into one — Part 1's batching move; the fused claim holds only if every piece does, bar $1/|\mathbb F|$.
    <div class="mt-3 flex items-baseline gap-2.5 text-neutral-600 dark:text-neutral-300"><span class="shrink-0 text-neutral-400 dark:text-neutral-600">←</span><span>queries $x_1, \dots, x_t \leftarrow [m]$, batching $\boldsymbol \varepsilon$</span></div>
  </div>

  <div data-step class="py-4">
    <div class="mb-2 text-sm font-medium tracking-wide text-neutral-400 dark:text-neutral-500">Step 5 &nbsp;·&nbsp; Verifier</div>
    Read $f(x_1), \dots, f(x_t)$ — the only touches of the old oracle. By the generator split from earlier, each read $f(x) = \langle \boldsymbol m, G^{\mathrm{msg}}_{\mathcal C}[x] \rangle + \langle \boldsymbol r, G^{\mathrm{rnd}}_{\mathcal C}[x] \rangle$ is a linear constraint on $(\boldsymbol m, \boldsymbol r)$ — exactly the shape the joint constraint accepts. Assemble the output instance:
    <div class="mt-2 space-y-1 text-neutral-500 dark:text-neutral-400">
      <div><span class="mr-2 inline-block w-20 text-neutral-400 dark:text-neutral-600">target</span> $\mu' := \mu + \sum_i \varepsilon_i \cdot y_i + \sum_{i,l} \varepsilon_{\dots} \cdot f(x_i)_l$</div>
      <div><span class="mr-2 inline-block w-20 text-neutral-400 dark:text-neutral-600">oracles</span> $g$, the old masks $(\xi_i)$, the new mask $s$</div>
      <div><span class="mr-2 inline-block w-20 text-neutral-400 dark:text-neutral-600">constraint</span> one joint constraint over $(\mathcal C', \mathcal C_{\mathsf{zk}}, \mathsf{sl}')$, packing four linear pieces:</div>
    </div>
    <div class="mt-2 space-y-1 border-l-2 border-neutral-200 dark:border-neutral-800 pl-4 text-neutral-500 dark:text-neutral-400">
      <div>the old main constraint $\langle \boldsymbol m, \mathsf{sl}(\mathsf{st}) \rangle$, carried by $\mathsf{sl}'$ with state $\mathsf{st}' := (\mathsf{st}, \boldsymbol \varepsilon, \rho, x_1, \dots, x_t)$</div>
      <div>the carried mask constraints $\langle \boldsymbol \xi_i, \boldsymbol u_i \rangle$</div>
      <div>the joint OOD constraint $\sum_i \varepsilon_i \cdot \langle (\boldsymbol m, \boldsymbol r, \boldsymbol s), \mathsf{ze}_i(\rho) \rangle$</div>
      <div>the consistency reads $\sum_{i,l} \varepsilon_{\dots} \cdot \langle (\boldsymbol m, \boldsymbol r), G_{\mathcal C}[x_{i,l}] \rangle$</div>
    </div>
    <div class="mt-3 text-neutral-500 dark:text-neutral-400">Same shape, one size smaller — feed it back to step 1.</div>
  </div>

</ProtocolStepper>

<img
  src="/diagrams/zk-whir-code-switch-flow.svg"
  alt="Message-flow diagram for the code-switch IOR: prover sends g = Enc_{C'}(m, r') and mask s = Enc_{C_zk}((s, r), r''); verifier sends OOD randomness ρ; prover replies with joint OOD answer y = ze(ρ) · (m, r, s); verifier issues in-domain consistency queries x_1,…,x_t and batching scalars ε, reads f(x_i), and assembles μ' that bundles the original main-oracle constraint, the mask-oracle constraints, the joint OOD constraint, and the consistency constraints into the next committed sumcheck instance over (C', C_zk, sl')."
/>

**A counterintuitive efficiency property:** the prover and verifier time of this code-switch are _independent of the starting code $\mathcal C$_. The dominant cost is the encoding under $\mathcal C'$ (the new, smaller code) and the verifier's computation of $\mu'$. The starting code's structure pushes through into the next relation's succinct linear form $\mathsf{sl}'$ — captured by a quantity $T_{\mathcal C}$ the paper calls the **code-switching friendliness** of $\mathcal C$ — but doesn't show up in this protocol's local cost. People reach for "the slow code makes the switch slow"; that's not what happens.

## From building blocks to the theorems

Compose the HVZK sumcheck reduction (Part 1) with the code-switch IOR above, all of it under the composable HVZK-for-IORs framework, and three named theorems fall out. The composition is sound exactly because each IOR explicitly handles the "future distinguisher" — namely, the simulator of whatever IOR consumes its output — so HVZK chains end-to-end.

### Theorem 1 — sublinear HVZK IOPP

Compose two things: the HVZK sumcheck reduction (Part 1's §6), reducing $\mathcal R_{\mathcal C^{\equiv 2^k}, \mathcal C_{\mathsf{zk}}, \mathsf{sl}}$ to $\mathcal R_{\mathcal C, \mathcal C_{\mathsf{zk}}, \mathsf{sl}'}$; followed by a non-succinct base-case HVZK IOPP (a generalization of the simple $2\times$-overhead protocol from §2.2 of the paper, applied at a much smaller scale).

The **sublinear regime** comes from picking $2^k \approx m_{\mathcal C}$ — i.e. setting the interleaving factor to roughly the codeword block length. Then the encoded message in $\mathcal C^{\equiv 2^k}$ has size $\ell = 2^k \cdot \ell'$ with $\ell' \approx \sqrt{\ell}$, the post-reduction instance is $\sqrt{\ell}$-sized, and the base-case IOPP (which _does_ pay $2\times$ overhead) runs on a $\sqrt{N}$-size sub-instance. Total proof length: $\approx O(\sqrt{N})$ alphabet symbols. Verifier time: sublinear. Overhead vs the non-private IOPP: $o(1)$ — because the $2\times$ from the base case is amortized over a $\sqrt{N}$-size piece of the original $N$-size problem.

The full mask in the base case is _affordable only because_ it runs on the small piece. That asymmetry is how this paper buys sublinear masking globally.

### Theorem 2 — HVZK code-switching

The full reduction from $\mathcal R_{\mathcal C_1^{\equiv 2^k}, \mathcal C_{\mathsf{zk}}, \mathsf{sl}_1}$ to $\mathcal R_{\mathcal C_2, \mathcal C_{\mathsf{zk}}, \mathsf{sl}_2}$, for _arbitrary_ codes $\mathcal C_1, \mathcal C_2$ that can themselves be interleaved. Composition: run the HVZK sumcheck reduction first (drops interleaving), then run the code-switch IOR (drops to $\mathcal C_2$). HVZK with respect to a bounded distinguisher class; RBR knowledge soundness with error $\leq 2^{-\lambda}$ at suitable field size.

This is the engine of the polylog-verifier theorem.

### Theorem 4 — polylogarithmic HVZK verifier

Iterate Theorem 2 along a chain of codes $\mathcal C_1, \mathcal C_2, \dots, \mathcal C_n$, picked so that the $2^k$-interleaving of $\mathcal C_{i+1}$ has the same message size as $\mathcal C_i$ (non-interleaved). Each application of Theorem 2 reduces the problem size by a factor of $2^k$; after $n$ steps the problem is small enough to finish with a small variant of the §2.2 base case. Theorem 4 is parametrized over an arbitrary chain length; specific instantiations (next subsection) pin $n$.

This is the WHIR/Ligerito recursion shape — same idea, ZK-augmented. (See the [WHIR post](/writing/whir) for the non-ZK version of this recursion.)

### Corollary 3 — HVZK Reed–Solomon ⇒ HVZK WHIR/Ligerito

Instantiate Theorem 4's chain with Reed–Solomon codes:

- $k := O(\log \log \ell)$ (folding factor per step).
- Each $\mathcal C_i$ is Reed–Solomon over an evaluation domain of size $\approx 2^{-i} m$.
- $\mathcal C_{\mathsf{zk}}$ instantiated with Reed–Solomon at rate $1/\log \lambda$, mask size $\widetilde O(\lambda)$.

This is the parameter regime of WHIR ([ACFY25]) and Ligerito ([NA25]), with ZK encodings substituted for the plain encodings throughout. The result:

- **$O(m) + \widetilde O(\lambda)$** alphabet symbols sent by the prover; verifier makes $O(\lambda)$ queries.
- **RBR knowledge soundness error $\leq 2^{-\lambda}$.**
- **Perfect honest-verifier zero-knowledge** ($\zeta = 0$, thanks to RS having perfect ZK encodings with appended randomness).
- Proof length and query complexity match the non-private WHIR/Ligerito up to $o(1)$ overhead.

**This is the headline.** Theorem 2 plus the building blocks above _directly yield an HVZK variant of WHIR and Ligerito_ — a drop-in zero-knowledge replacement for the code-agnostic IOP family at $1 + o(1)$ overhead, in the list-decoding regime, with negligible RBR soundness error. The first such result.

### Comparison

| Protocol                     | Overhead            | Soundness regime    | Code family                              | Verifier            |
| ---------------------------- | ------------------- | ------------------- | ---------------------------------------- | ------------------- |
| [BCL22]                      | $O(1)$              | Unique decoding     | Any ZK code (tensor-style)               | Polylog             |
| [RW24]                       | $1 + o(1)$          | Constant error only | ZK tensor codes                          | —                   |
| Naive full-mask ZK WHIR      | $\geq 2\times$      | List decoding       | Reed–Solomon only                        | Sublinear / polylog |
| **HVZK WHIR (this paper)**   | $\mathbf{1 + o(1)}$ | **List decoding**   | **Any $\mathbb F$-additive ZK code**     | **Polylog**         |

The two rows that previously each owned one of these properties no longer dominate. This work is the first to put all four boxes ticked at once.

## R1CS reduction with sublinear masking

The IOPP above tests proximity to constrained interleaved codes. To turn it into an end-to-end HVZK argument for NP, you reduce R1CS to that proximity test. The paper does this with $1 + o(1)$ overhead by reusing the same masking idea.

Backbone: an interleaved variant of [ACFY25]'s $\Sigma$-IOP for R1CS. Standard R1CS-to-sumcheck-to-codeword pipeline, but with two types of sublinear masks plugged in:

- **R1CS witness masks.** Protect the matrix-vector products $A \cdot z$, $B \cdot z$, $C \cdot z$ from leaking. [BCL22] handles this by padding the R1CS instance with full-size random witness — $O(1)$ overhead. This paper uses sublinear masks instead — $o(1)$.
- **Sumcheck masks.** Same as Part 1's §6 — small per-round masks accumulated into the joint constraint.

Both mask types are syntactically identical from the relation's perspective — they propagate into an instance of $\mathcal R_{\mathcal C, \mathcal C_{\mathsf{zk}}, \mathsf{sl}}$ which our protocols can test in zero-knowledge end-to-end. Compose with Theorem 1 or Theorem 4 and you get an HVZK IOP for NP with list-decoding soundness and $o(1)$ overhead over sending a single interleaved codeword — where [BCL22] pays a constant factor and only reaches unique-decoding soundness.

One honest caveat: the verifier here is _non-succinct_ on R1CS-side cost, because this paper deliberately does not employ holographic preprocessing (the way [BCL22] and [RW24] do). The IOPP itself remains polylog-verifier.

If you want background on the R1CS-to-sumcheck step, [Spartan](/writing/spartan-protocol) and [SPARK](/writing/spark) cover it.

## What we didn't cover

A few honest cuts.

**High-distance codes from dispersers (Section 12 of the paper).** The protocols here are code-agnostic, so you can buy more distance via the ABNNR construction [ABNNR92] — distance amplification through bipartite-disperser graphs. Plugged into Reed–Solomon, this gives **amplified Reed–Solomon** (ARS): RS at a low-cost base distance, then distance-boosted by gathering disperser neighbors. ARS shaves encoding time in the regime where plain RS pays $O(\ell/\gamma \cdot \log(\ell/\gamma))$ for FFT-based evaluation, replacing it with $O(\ell/\gamma + \ell \log \ell)$. Cost: alphabet inflates by a factor of $d = O(1/\gamma)$, so spot-checks cost more bytes per opening. Under reasonable parameters the paper reports a ≈ 30% argument-size win. Orthogonal to the ZK story and worth a separate read.

**Round-by-round knowledge soundness proofs.** We stated the errors qualitatively but skipped the full mutual-correlated-agreement accounting (the paper's §6 and §9). [WHIR](/writing/whir) treats MCA and list-decoding-regime soundness analysis for the non-ZK case; the ZK accounting is structurally similar but adds extra terms for the mask oracles.

**Memory-efficient streaming prover.** The paper notes that Theorem 1 is plausibly amenable to space-efficient streaming proving (à la [BBHV22], [CTY11]) — the encoding of $\mathcal C^{\equiv K}$ can be done in space proportional to a single base encoding, and the sumcheck can stream too. Not pursued in the paper but flagged as future work.

**Malicious-verifier ZK.** Out of scope. Fiat–Shamir turns HVZK into malicious-verifier ZK for free in the non-interactive setting, which is what all deployed hash-based SNARKs care about.

## Conclusion

The construction lands on five interlocking moves:

1. **The committed sumcheck relation** — succinct linear forms plus mask oracles folded into a joint constraint, set up so the protocol can carry mask state through composition.
2. **Composable HVZK for IORs** — the distinguisher class $\mathcal D$, the composition theorem, and the discipline of making "future queries" explicit so naive composition stops shipping the witness.
3. **HVZK sumcheck via interleaved folding** — small per-round masks, encode-then-fold = fold-then-encode, and correlated queries over the bigger alphabet to keep list-decoding-regime soundness affordable.
4. **Private zero-evaders for ZK-compatible OOD** — randomized OOD samples that still pin a unique message while looking uniform to the verifier, plus the joint OOD sample over the message and an extra mask piece.
5. **Code-agnostic assembly** — Theorems 1/2/4 plus the RS instantiation give the first HVZK variant of WHIR and Ligerito.

The practical upshot is concrete. Anonymous-credential stacks like [Fs24], built on the sublinear hash-based zkSNARK in [AHIV17], pay $\geq 2\times$ overhead today because the underlying IOP is private but old. The corresponding _non-private_ IOPs are no longer state of the art. This work gives a drop-in path: swap the old private IOP for an HVZK WHIR/Ligerito built from the protocols above, eat $1 + o(1)$ overhead instead of $\geq 2\times$, and the effect compounds because we're measuring against the modern non-private state of the art rather than the 2017 baseline.

The longer-term effect is structural. The code-agnostic IOP family was the wrong place to look for ZK because the existing zero-knowledge moves were tied to polynomial codes. This paper unties them. Once the framework is in place — ZK encodings, composable HVZK-for-IORs, sublinear masking, private OOD — the next ZK variant of the next code-agnostic IOP is a much shorter walk.

## References

- **This paper** — Chiesa, A., Fenzi, G., & Weissenberg, G. (Feb 2026). _Zero-Knowledge IOPPs for Constrained Interleaved Codes._
- **WHIR** — Arnon, G., Chiesa, A., Fenzi, G., & Yogev, E. (2025). _WHIR: Reed–Solomon Proximity Testing with Super-Fast Verification._ EUROCRYPT '25.
- **Ligerito** — Novakovic, A., & Angeris, G. (2025). _Ligerito: A Small and Concretely Fast Polynomial Commitment Scheme._ [eprint 2025/1187](https://eprint.iacr.org/2025/1187)
- **BCFW25** — Bünz, B., Chiesa, A., Fenzi, G., & Wang, W. (2025). _Linear-Time Accumulation Schemes._ TCC '25.
- **BCL22** — Bootle, J., Chiesa, A., & Liu, S. (2022). _Zero-Knowledge IOPs with Linear-Time Prover and Polylogarithmic-Time Verifier._ EUROCRYPT '22.
- **RW24** — Ron-Zewi, N., & Weiss, M. (2024). _Zero-knowledge IOPs Approaching Witness Length._ CRYPTO '24.
- **RR20** — Ron-Zewi, N., & Rothblum, R. (2020). _Local Proofs Approaching the Witness Length._ FOCS '20.
- **RR22** — Ron-Zewi, N., & Rothblum, R.D. (2022). _Proving as Fast as Computing: Succinct Arguments with Constant Prover Overhead._ STOC '22.
- **BMMS25** — Baweja, A., Mishra, P., Mopuri, T., & Shtepel, M. (2025). _FICS and FACS: Fast IOPPs and Accumulation via Code-Switching._ [eprint 2025/737](https://eprint.iacr.org/2025/737)
- **ABNNR92** — Alon, N., Bruck, J., Naor, J., Naor, M., & Roth, R.M. (1992). _Construction of asymptotically good low-rate error-correcting codes through pseudo-random graphs._ IEEE TIT 38(2).
- **AHIV17 (Ligero)** — Ames, S., Hazay, C., Ishai, Y., & Venkitasubramaniam, M. (2017). _Ligero: Lightweight Sublinear Arguments Without a Trusted Setup._ CCS '17.
- **Fs24** — Frigo, M., & Shelat, A. (2024). _Anonymous credentials from ECDSA._ [eprint 2024/2010](https://eprint.iacr.org/2024/2010)
- **BBHV22** — Bangalore, L., Bhadauria, R., Hazay, C., & Venkitasubramaniam, M. (2022). _On Black-Box Constructions of Time and Space Efficient Sublinear Arguments from Symmetric-Key Primitives._ TCC '22.
- **CTY11** — Cormode, G., Thaler, J., & Yi, K. (2011). _Verifying computations with streaming interactive proofs._ VLDB.
- **ACFY25** — Arnon, G., Chiesa, A., Fenzi, G., & Yogev, E. (2025). _WHIR: Reed–Solomon Proximity Testing with Super-Fast Verification._ EUROCRYPT '25.
- **Companion blogs.** [WHIR](/writing/whir), [TensorSwitch I](/writing/tensorswitch-part-1), [TensorSwitch II](/writing/tensorswitch-part-2), [Spartan](/writing/spartan-protocol), [SPARK](/writing/spark), [Part 1 of this series](/writing/zk-whir-part-1).
